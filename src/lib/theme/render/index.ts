// Theme → slide projection for the type-first model (themeredo.md, Phase 1).
// Renders theme previews through the shared slide renderer.

export { themeToSlide } from "./theme-to-slide"
export { slideToTheme, type ThemeIdentity } from "./slide-to-theme"
