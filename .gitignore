# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Build artifacts
build
data/lumenlive.db
data/sources/*.json
data/cross-refs/cross_references.txt
data/cross-refs/cross-references.zip
data/verses-for-embedding.json

# SQLite transient sidecars — machine-specific, never commit
data/*.db-shm
data/*.db-wal
# Bible source bundle — downloaded from R2 at setup, not a source artifact
data/bible-sources.zip
# Resource-store build output — regenerated by build:store-manifest, uploaded to R2
data/store-artifacts/
# Per-translation export staging (upload only) — never commit; holds copyrighted text
temp-bible/

# Python virtual environment
.venv/

# ML models and embeddings (downloaded, not committed)
models/*
.env
.venv/
.superpowers/
embeddings/*

.claude/CLAUDE.md
# Personal/local Claude Code settings — machine-specific, never commit
.claude/settings.json
.claude/settings.local.json

# npm lockfile — this project uses bun (bun.lock is the source of truth)
package-lock.json

# Local knowledge/scratch docs — not part of the published repo
Knowledge/

detailed-plan.md

docs/superpowers
data/bg_temp
GHS-1-260.md
ghs.json

__pycache__/

# Playwright MCP server output — tool-generated, machine-specific
.playwright-mcp/

# GSD planning artifacts
.planning/
# Full store upload staging (incl. copyrighted) — never commit
data/store-upload/
